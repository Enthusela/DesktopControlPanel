# Script to generate a response to a given input using the chatGPT model
# Convert the response to an audio file and play it

import openai
import os

openai.ChatCompletion.create(
    model="gpt-chat",
    messages=
)